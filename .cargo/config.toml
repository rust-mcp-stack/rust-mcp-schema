[alias]

coverage = ["tarpaulin"]
run_clippy = [
    "clippy",
    "--features",
    "latest schema_utils",
    "--fix",
    "--",
    "-A",
    "deprecated",
    "-D",
    "warnings",
]

run_test = ["nextest", "run"]
